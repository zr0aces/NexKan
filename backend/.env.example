# This file is superseded by the root .env.example.
# See: cp .env.example .env
#
# Kept for reference — the root .env is read by both docker-compose
# (for HOST_PORT substitution) and the backend container (via env_file).
